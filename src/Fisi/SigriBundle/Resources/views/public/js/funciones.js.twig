{# empty Twig template #}
<script>
    $(document).ready(function () {  
        
         registrarCategoriaV();
    });
    
    function cambiar(id, nombre){
       $("#tdCambiar"+id).attr("value",nombre);      
        
    }
    
    function agregarFilaActGestor() {
             i = 1;

        $(".clsFilaActividad").each(function() {
            i++;
        });

        if ($('#trFilaActividad' + i).length) {
            i++;
        }
        
        $.ajax({
            type: "POST",
            url: "{{path('agregarFilaGestor')}}",
             data: {
                fila: i
            }
            
        }).done(function(msg) {
            $("#tBodyAgregarAct").append(msg);
         
        });
    }
    
  
    function eliminarFila(nrofila) {
        $("#trFilaActividad" + nrofila).remove();
        alert("Se elimino fila");
    }

    function agregarFilaRespGestor() {
             i = 1;

        $(".clsFilaResp").each(function() {
            i++;
        });

        if ($('#trFilaResponsable' + i).length) {
            i++;
        }
        
        $.ajax({
            type: "POST",
            url: "{{path('agregarFilaRespGestor')}}",
             data: {
                fila: i
            }
            
        }).done(function(msg) {
            $("#tBodyAgregarResp").append(msg);
         
        });
    }
    
    function registrarCategoriaV() {
            
        
        $.ajax({
            type: "POST",
            url: "{{path('registrarCategoriaV')}}"
            
        }).done(function(msg) {
            $("#dRegistro").html(msg);
         
        });
    }
    
    function editarCategoriaV() {
            
        alert("Editar");
        $.ajax({
            type: "POST",
            url: "{{path('editarCategoriaV')}}"
        }).done(function(msg) {
            alert("Editar dentro");
            $("#dRegistro").html(msg);
         
        });
    }
    
    function eliminarCategoriaV() {
           alert("Eliminar"); 
        
        $.ajax({
            type: "POST",
            url: "{{path('eliminarCategoriaV')}}"
         
            
        }).done(function(msg) {
            $("#dRegistro").html(msg);
         
        });
    }
  
    
   
    	</script>